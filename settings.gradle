pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //para o projeto de qrcode
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Minha Lista de Compras"
include ':app'
